{
  "name": "stripe-webhook",
  "version": "0.0.1",
  "description": "A webhook for Astro's subscriptions.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "updateLambda": "zip -r dist.zip index.js package.json node_modules; aws lambda update-function-code --function-name stripe-webhook --zip-file 'fileb://dist.zip'"
  },
  "author": "ASTRO",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "qs": "^6.11.2",
    "side-channel": "^1.0.5",
    "stripe": "^14.15.0"
  }
}