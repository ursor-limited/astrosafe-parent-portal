import { AstroAccountTab } from './contents/common';
declare const Profile: ({ params, searchParams, }: {
    params: {
        id: string;
    };
    searchParams: {
        tab: AstroAccountTab;
    };
}) => import("react/jsx-runtime").JSX.Element;
export default Profile;
